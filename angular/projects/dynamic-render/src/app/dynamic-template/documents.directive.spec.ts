import { DocumentsDirective } from './documents.directive';

describe('DocumentsDirective', () => {
  it('should create an instance', () => {
    // const directive = new DocumentsDirective();
    // expect(directive).toBeTruthy();
  });
});
