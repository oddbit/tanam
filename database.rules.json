{
  "rules": {
    "tanam": {
      "$siteId": {
        "tasks": {
          "cache": {
            "$action": {
              "$taskId": {
                ".write": "(auth != null && auth.token.tanam[$siteId][0].length > 0)"
              }
            }
          }
        }
      }
    }
  }
}
