{
  "database": {
    "rules": "database.rules.json"
  },
  "functions": {
    "predeploy": [
      "npm --prefix functions run build",
      "npm --prefix functions run package"
    ],
    "source": "dist"
  },
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  },
  "hosting": {
    "public": "angular/dist",
    "ignore": [
      "firebase.json",
      "**/.*",
      "**/node_modules/**"
    ],
    "rewrites": [
      {
        "source": "favicon.ico",
        "function": "_/assets/favicon.ico"
      },
      {
        "source": "**",
        "function": "tanam"
      }
    ]
  },
  "storage": {
    "rules": "storage.rules"
  }
}
